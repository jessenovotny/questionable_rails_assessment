<%= button_to("#{question.favorited_by?(current_user)} | #{question.favorite_count}", question_favorites_path(question), form: {class: "inline_button favorite", id: "question-#{question.id}"}) if logged_in? %>