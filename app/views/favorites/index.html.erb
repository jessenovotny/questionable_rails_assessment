<h1>

  Showing <%= @user.favorited_questions.count %> questions favorited by <%= @user.username %>:

</h1>

<br>

<%= render @questions %>

<%= link_to("Delete Account", user_path(current_user), method: 'delete', data: { confirm: 'Are you sure?' }) if current_user == @user %>