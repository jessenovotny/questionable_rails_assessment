<h1>New Question</h1>

<%= form_for(@question, url: user_questions_path(current_user)) do |f| %>

  <div class="field-with-errors">
    <%= text_area_tag 'question[content]', "Ask your question here:", rows: 5, cols: 30%>
  </div>

  <div class="checkbox_container">
    <%= collection_check_boxes(:question, :category_ids, Category.all_for(@question), :id, :name) %>
  </div>

  <%= text_field_tag "question[new_category_attribute][name]", nil, placeholder: "Create New Category" %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', questions_path %>
